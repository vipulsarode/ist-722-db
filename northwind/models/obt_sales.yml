version: 2

models:
  - name: obt_sales
    description: The sales fact table combined with all of its outrigger dimensions into one bit table. Transaction fact table. One row per line item ordered. Includes product data.
